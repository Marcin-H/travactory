import FlightsList from '../FlightsList.tsx';
import './style.scss';

const Flights = () => {
  return (
    <div className="container">
      <FlightsList />
    </div>
  );
};

export default Flights;
